# Setup

1. Clone the repo:
   ```
   git clone <repo-url>
   cd <repo-name>
   ```
2. Install dependencies:
   ```
   uv sync
   ```
3. Install pre-commit hooks:
   ```
   pre-commit install
   ```
4. Run the test suite:
   ```
   pytest -q
   ```
