## Declared direction

<!-- declared-direction: one-sentence summary of this PR's product-direction intent -->

Quire (automated PR triage) groups related work by reading the `declared-direction`
marker above — fill in the sentence, keep the HTML comment syntax intact. PRs missing
it still get triaged, just ungrouped from related work.

## Changes

## Test plan

- [ ] `npm run build` passes
- [ ] `npm run dev` — checked the change in a browser
